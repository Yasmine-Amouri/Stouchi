all:
  hosts:
    stouchi-prod-server:
      ansible_host: "{{ lookup('pipe', 'terraform -chdir=../terraform/azure output -raw vm_public_ip') }}"
      ansible_user: deploy
      ansible_ssh_private_key_file: ~/.ssh/stouchi_vm